version: "3.8"

services:
  db:
    container_name: footballscoreboard_db
    image: mysql:8.0.28
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: football_db
    volumes:
      - path/to/mysql:/var/lib/mysql
      - ./data:/docker-entrypoint-initdb.d
    networks:
      - dbnet

  appdb_viewer:
    container_name: footballscoreboard_dbviewer
    image: adminer
    ports:
      - 8080:8080
    networks:
      - dbnet
    depends_on:
      - db

networks:
  dbnet:
    driver: bridge
